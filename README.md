# solovieva
# Expense Tracker

**Автор:** Соловьева Виктория

## Описание программы

Expense Tracker — простое приложение для учёта личных расходов с графическим интерфейсом. Позволяет добавлять расходы по категориям и датам, фильтровать записи и считать сумму за выбранный период. Данные сохраняются в формате JSON.

## Как использовать

1. Установите Python 3.x.
2. Скопируйте файлы проекта в одну папку.
3. Запустите main.py.
4. Введите сумму (положительное число), выберите категорию и дату (ГГГГ-ММ-ДД), нажмите «Добавить расход».
5. Для фильтрации используйте вкладку «Фильтр».
6. Для подсчёта суммы за период — вкладку «Сумма за период».
7. Данные сохраняются автоматически.

## Примеры использования

- Добавить расход: 100 ₽ на еду 2026-04-24.
- Посчитать сумму расходов за апрель 2026 года.
- Отфильтровать расходы по категории «Транспорт».
```

## 5. Использование Git

1. Откройте терминал в папке проекта.
2. Инициализируйте репозиторий:
   ```
   git init
   ```
3. Добавьте файлы:
   ```
   git add .
   ```
4. Сделайте первый коммит:
   ```
   git commit -m "Initial commit"
   ```
5. (Опционально) Создайте репозиторий на GitHub/GitLab и залейте проект.
```
git remote add origin <ваш_репозиторий>
git push -u origin master
```
```

### Примечания

- Приложение проверяет корректность ввода суммы и даты.
- Данные сохраняются в `expenses.json`.
- Можно расширить категории и добавить новые функции.
- Для работы требуется Python 3.x и библиотека tkinter (обычно уже установлена).
